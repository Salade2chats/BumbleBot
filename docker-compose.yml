version: '3'
services:
  app:
    container_name: bumblebot
    build:
      context: .
      dockerfile: docker/Dockerfile
      args:
        UID: $UID
        GID: $GID
    volumes:
      - '.:/data'
    env_file:
      - .env
    ports:
      - 9042:$PORT
    user: $UID:$GID
    command: ["yarn", "run", "serve"]
